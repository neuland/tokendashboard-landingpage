# Where the "Decisions" section reads the backend's decision log from. Defaults to the
# main branch of neuland/tokendashboard-backend on GitHub; point it at a local checkout
# to preview an uncommitted log:
# DECISIONS_SOURCE=../tokendashboard-backend/docs/decisions.md
